public String convert15to18DigitId(String fifteenDigit) {
    if (fifteenDigit == null) {
        return null;
    } else if (fifteenDigit.size() < 15) {
        return -1;    
    }
    
    Id converted15Digit = fifteenDigit;
    return converted15Digit == NULL ? -1 : converted15Digit;
}