public Boolean isPrime(Integer num) {
    Boolean itsPrime = true;

    for(Integer i = 2;i < num - 1; i++){
        if(Math.mod(num, i) == 0){
            itsPrime = false; 
            break;
        }
    }
    
    return itsPrime;
}