public Integer fibonacci(Integer n) {
    
    List<Integer> fib = new List<Integer>{1, 1};
    // 2 > 1
    if (n > 2) {

        for (Integer i = 0; i < n; i++){
            // fib[2] = fib[2-1] + fib[2-2] = 2
            fib[i] = fib[i-1] + fib[i-2];
        }
        return fib[n-1];
    } else {
        return 1;
    }

}